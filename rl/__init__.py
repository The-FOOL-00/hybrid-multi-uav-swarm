"""
rl/ — Reinforcement Learning module for Hybrid Multi-UAV Swarm.

Status: SKELETON — not yet active in simulation.
        Controllers in controllers/ handle all live Webots behavior.

Planned sub-modules:
    gym_wrapper  — OpenAI Gym-compatible environment wrapping the Webots sim
    rewards      — Modular reward function library
    training     — Training loop and experiment management
    algorithms   — Algorithm configs and entry points (PPO, MAPPO, etc.)

Usage (future):
    from rl.gym_wrapper import UAVSwarmEnv
    from rl.rewards import CoverageReward
"""
